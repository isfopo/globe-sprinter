import { AddCommandModal } from "./AddCommandModal";
import { AddDirectoryModal } from "./AddDirectoryModal";

export const Modals = () => {
  return (
    <>
      <AddDirectoryModal />
      <AddCommandModal />
    </>
  );
};
